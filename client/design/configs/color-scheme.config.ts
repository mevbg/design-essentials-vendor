import {
  ColorSchemeConfig,
  ColorSchemeMethod,
  ColorSchemeType,
} from '../../../src/types/design.types.js';

export const colorScheme: ColorSchemeConfig = {
  default: ColorSchemeType.LIGHT,
  method: ColorSchemeMethod.COMBINED
};
